# AutoFillIn MCP Configuration
# Add to your MCP settings (Claude Code, OpenCode, etc.)

# For Claude Code (~/.config/claude-code/settings.json or similar)
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-playwright"]
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/"]
    }
  }
}

# For VS Code Copilot - Install these extensions:
# - vijaynirmal.playwright-mcp-relay
# - vijaynirmal.chrome-devtools-mcp-relay

# Environment variables for shell integration:
export CHROME_DEBUG_PORT=9222
export CHROME_USER_DATA_DIR="$HOME/.chrome-autofillin"

# To manually start Chrome with debug mode:
# /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
#   --remote-debugging-port=9222 \
#   --user-data-dir="$HOME/.chrome-autofillin" \
#   "https://example.com"
